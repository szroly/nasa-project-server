{
  "name": "nasa-project-api",
  "version": "1.0.0",
  "description": "NASA Mission controll API",
  "main": "src/server.js",
  "scripts": {
    "test": "node --env-file=.env node_modules/.bin/jest --runInBand",
    "test-watch": "jest --watchAll",
    "watch": "node --watch src/server.js",
    "start": "node --env-file=.env src/server.js",
    "cluster": "pm2 start src/server.js -i max"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "jest": {
    "testEnvironment": "node"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parse": "^6.1.0",
    "express": "^5.2.1",
    "mongoose": "^9.1.5",
    "morgan": "^1.10.1",
    "pm2": "^6.0.14"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "supertest": "^7.2.2"
  }
}
